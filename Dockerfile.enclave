#!/bin/bash

FROM ghcr.io/astral-sh/uv:python3.11-bookworm-slim

WORKDIR /app

ENV UV_NO_DEV=1 \
    UV_COMPILE_BYTECODE=1 \
    PATH="/app/.venv/bin:$PATH"

COPY . .

RUN uv sync --frozen

EXPOSE 8001
CMD ["uvicorn", "enclave.main:app", "--host", "0.0.0.0", "--port", "8001"]
